interface:
  display_name: "myagents"
  short_description: "Promote global Codex rules into AGENTS.md"
  default_prompt: "Use $myagents to add this as a polished global Codex rule:"

policy:
  allow_implicit_invocation: false
