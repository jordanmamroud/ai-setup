# mydoc-readme

Canonical runtime instructions now live in `SKILL.md`.

Keep this file only as a short handoff prompt for manual copy/paste:

> Use `$mydoc-readme` to create or refresh the current project's root `README.md`. Follow the skill's `SKILL.md` exactly, always include `Successful Outcome`, and preserve operational safety, verification, and run/deploy details when they are documented.
