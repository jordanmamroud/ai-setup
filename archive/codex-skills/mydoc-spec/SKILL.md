---
name: mydoc-spec
description: Explicit-only skill for creating a versioned project spec via the mydoc-spec guided interview. Use only when the user explicitly invokes $mydoc-spec.
---

# mydoc-spec

This skill is explicit-only. Do not use it unless the user invokes `$mydoc-spec` or selects it from `/skills`.

Read `references/instructions.md` and follow it exactly.
